#pragma once

#include "../../../../theme.h"

namespace pizda {
	using namespace yoba;
	using namespace yoba::ui;

	class NDControls : public Layout {
		public:
			NDControls() {
				setHeight(140);
			}
	};
}